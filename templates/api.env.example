# This is a protected envpilot environment file template.
# The real file is ~/.config/secrets/api.env and must remain mode 600 or 400.
# Managed interactive and non-interactive shells export every assignment here.
# Keep this file quiet and assignment-only; never commit the real file.

# export OPENAI_API_KEY="paste-your-key-here"
# export ALPHA_GENOME_API_KEY="REPLACE_ME"
# export NCBI_API_KEY="REPLACE_ME"
# export GITHUB_TOKEN="REPLACE_ME"
